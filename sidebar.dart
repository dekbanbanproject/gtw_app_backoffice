import 'package:flutter/material.dart';

class SidebarPages extends StatelessWidget {
  const SidebarPages({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}